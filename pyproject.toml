[project]
name = "artistscraper"
version = "0.1.0"
description = ""
authors = [
    {name = "lennarddevries",email = "lennard.devries@me.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ytmusicapi (>=1.11.1,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
